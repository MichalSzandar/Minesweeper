package michal.projects;

public enum State {
    /**means that Field has been marked with a flag. */
    MARKED,
    /**means that Field is hidden. */
    HIDDEN,
    /**means that Field is revealed. */
    REVEALED;
}

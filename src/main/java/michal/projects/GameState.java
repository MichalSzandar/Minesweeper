package michal.projects;

public enum GameState {
    /**means that Game is finished and player lost. */
    LOST,
    /**means that game is finished and player won. */
    WON,
    /**means that game is still going. */
    CONTINUES;
}
